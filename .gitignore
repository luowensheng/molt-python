builds/
ignore
.ignore
molt
dist/

# Claude Code session data (worktrees, memory) — excluded
# but project-level MCP config and onboarding guide are tracked
.claude/
!.claude/mcp.json
!.claude/CLAUDE.md