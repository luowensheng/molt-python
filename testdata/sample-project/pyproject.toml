[project]
name = "hello-molt"
version = "1.0.0"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.28.0",
]

[project.scripts]
hello-molt = "hello.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
