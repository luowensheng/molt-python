[project]
name = "demo-19-glue-go"
lang = "python"

# Glue configuration lives in stats.molt.toml — no inline [[tool.molt.glue]] needed.
# molt sync detects *.molt.toml files with a `lang` key and treats them as glue modules.

[tool.molt.tasks]
demo = "python main.py"
