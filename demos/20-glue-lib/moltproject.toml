[project]
name = "demo-20-glue-lib"
lang = "python"

# Glue configuration lives in gosha256.molt.toml and sha3.molt.toml.
# molt sync detects *.molt.toml files with a `lang` key and treats them as glue modules.

[tool.molt.tasks]
demo = "python main.py"
