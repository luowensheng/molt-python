[project]
name = "demo-21-glue-rust"
lang = "python"

# Glue configuration lives in compress.molt.toml.
# molt sync detects *.molt.toml files with a `lang` key and treats them as glue modules.

[tool.molt.tasks]
demo = "python main.py"
